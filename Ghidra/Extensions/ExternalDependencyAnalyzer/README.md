# External Dependency Analyzer

Scans defined strings and imported symbols for external dependency indicators,
including hostnames, URLs, ports, JDBC strings, queue endpoints, and TLS or
authentication indicators. Findings include source addresses, containing
functions, library names, and thunk attribution, and can be exported as JSON
or Markdown using the bundled Ghidra script.
